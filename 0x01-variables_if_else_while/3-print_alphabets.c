#include <stdio.h>
/**
 * main - print the alphabet in lowercase,and then in uppercase
 *
 * Return: 0
 */
int main(void)
{
	char ch;

	for (ch = 'a'; letter <= 'z', ch++)
	putchar(ch);

	for (ch = 'A'; letter <= 'Z'; ch++)
	putchar(ch);

	putchar('\n');
	return (0);
}
