#include "main.h"
/**
 * *_strncpy - copy a string
 * @dest: character one
 * @src: character two
 * @n: integer
 * Return: 0
 */
char *_strncpy(char *dest, char *src, int n)
{
	int i;

	i = 0;
	while (s1[i] != '\0' && s2[i] != '\0')
	{
	if (s1[i] != s2[i])
		{
	return (s1[i] = s2[i]);
			}
			i++;
			return (0);
			}
			}
