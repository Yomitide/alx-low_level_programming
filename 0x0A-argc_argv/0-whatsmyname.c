#include <stdio.h>
#include "main.h"
/**
 * main - the main program
 * @argc: the arguement
 * @argv: the pointer to the array
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
		return (0);
}
